# ==============================================================================
# 前端环境变量模板
# 复制为 .env 后按需修改：cp .env.example .env
# 开发时后端地址在本文件配置；构建产物走 .env.prod（VITE_SERVICE_BASE_URL=/api/v1 由 Nginx 代理）
# ==============================================================================

# the base url of the application, the default is "/"
# if use a sub directory, it must be end with "/", like "/admin/" but not "/admin"
VITE_BASE_URL=/

VITE_APP_TITLE=AI 助理中心

VITE_APP_DESC=基于 Deep Agents 的完整 Agent 平台

# the prefix of the icon name
VITE_ICON_PREFIX=icon

# the prefix of the local svg icon component, must include VITE_ICON_PREFIX
# format {VITE_ICON_PREFIX}-{local icon name}
VITE_ICON_LOCAL_PREFIX=icon-local

# auth route mode: static ｜ dynamic
VITE_AUTH_ROUTE_MODE=dynamic

# static auth route home
VITE_ROUTE_HOME=ai_qa-glass

# default menu icon
VITE_MENU_ICON=mdi:menu

# whether to enable http proxy when is dev mode
VITE_HTTP_PROXY=N

# 自动登录（开发便捷，留空则关闭）：取值 Super / Admin / User
VITE_AUTO_LOGIN=

# vue-router mode: hash | history | memory
VITE_ROUTER_HISTORY_MODE=history

# 品牌变体：standard / generic（pack_deploy.py 打包时可交互覆盖）
VITE_BRAND=standard

# 后端服务基础 URL（开发模式指向本地后端；构建时以 .env.prod 为准）
VITE_SERVICE_BASE_URL=http://localhost:9999/api/v1

# other backend service base url
VITE_OTHER_SERVICE_BASE_URL=`{
  "demo": "http://localhost:9529"
}`

# success code of backend service, when the code is received, the request is successful
VITE_SERVICE_SUCCESS_CODE=0000

# logout codes of backend service, when the code is received, the user will be logged out and redirected to login page
# 4001: invalid token (decode error / generic token failure / missing token)
# 4002: invalid session (wrong token type / user not found)
VITE_SERVICE_LOGOUT_CODES=4001,4002

# modal logout codes of backend service, when the code is received, the user will be logged out by displaying a modal
# 4003: account has been disabled
VITE_SERVICE_MODAL_LOGOUT_CODES=4003

# token expired codes of backend service, when the code is received, it will refresh the token and resend the request
# 4010: access/refresh token expired
VITE_SERVICE_EXPIRED_TOKEN_CODES=4010

# when the route mode is static, the defined super role
VITE_STATIC_SUPER_ROLE=R_SUPER

# sourcemap
VITE_SOURCE_MAP=N

# Used to differentiate storage across different domains
VITE_STORAGE_PREFIX=SOY_

# used to control whether the program automatically detects updates
VITE_AUTOMATICALLY_DETECT_UPDATE=Y

# show proxy url log in terminal
VITE_PROXY_LOG=Y

# used to control whether to launch editor (dev mode only)
VITE_DEVTOOLS_LAUNCH_EDITOR=code

# 标准文档图片 / PDF URL 前缀（垂直业务专用，通用部署可留空）
VITE_STANDARD_IMAGE_PREFIX=
VITE_STANDARD_PDF_PREFIX=
